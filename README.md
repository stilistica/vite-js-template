# ⚡️ Vite JS Ttemplate

Готовий багатосторінковий шаблон на Vite, оптимізований для сучасної фронтенд-розробки. Підходить для проєктів, де потрібні кілька HTML-сторінок, SCSS, деплой на GitHub Pages.
Для налаштування додаткових можливостей [звернись до документації](https://vitejs.dev/).

---

## 🛠 Налаштування Vite

Шаблон уже готовий:
- Збірка: вихід в dist/.
- Багатосторінковість: підтримка index.html та інших сторінок у pages/.
- SCSS: робочий SCSS-процес.
- Компоненти: Handlebars + Hulak для реактивних HTML-компонентів.

## Файли і папки

- Файли розмітки компонентів сторінки повинні лежати в папці `src/components`.
- Файли стилів повинні лежати в папці `src/styles` та імпортуватись до JS-файлів
- Зображення додавай до папки `src/img`. Збирач оптимізує їх, але тільки при 
  деплої продакшн версії проекту. Все це відбувається у хмарі, щоб не
  навантажувати твій комп'ютер, тому що на слабких компʼютерах це може зайняти
  багато часу.

---

## Оновлення Settings

Після того, як репозиторій буде створено, необхідно перейти в налаштування
створеного репозиторію на вкладку `Settings` > `Actions` > `General` як показано
на зображенні.

![Settings GitHub Actions permissions step 1](./public/gh-actions-perm-1.png)

Проскроливши сторінку до самого кінця, в секції `«Workflow permissions»` обери
опцію `«Read and write permissions»` і постав галочку в чекбоксі. Це необхідно
для автоматизації процесу деплою проекту.

![Settings GitHub Actions permissions step 2](./public/gh-actions-perm-2.png)


### 🔧 Модулі JS

- Код можна розбивати на модулі у `js/modules/` та імпортувати їх у `main.js`.

---

## 🧠 Використання

### Встановлення залежностей

`bash => npm install`

---

### Запуск проєкту

`npm run dev`

### Для білду продакшн

`npm run build`

### Перегляд зібраної версії

`npm run preview`


## Деплой

Продакшн версія проекту буде автоматично збиратися та деплоїтись на GitHub
Pages, у гілку `gh-pages`, щоразу, коли оновлюється гілка `main`. Наприклад,
після прямого пуша або прийнятого пул-реквесту. Для цього необхідно у файлі
`vite.config.ts` змінити значення прапора `const repoBase = "/REPO/"`, для команди `build`,
замінивши `<REPO>` на назву свого репозиторію

```ts
const repoBase = "/REPO/";
```

Далі необхідно зайти в налаштування GitHub-репозиторію (`Settings` > `Pages`) та
виставити роздачу продакшн версії файлів з папки `/root` гілки `gh-pages`, якщо
це не було зроблено автоматично.

![GitHub Pages settings](./public/repo-settings.png)